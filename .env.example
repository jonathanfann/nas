# NAS file server root path (browse, upload, download)
NAS_BASE_PATH=/nas
# Optional: systemd service name used by in-app restart button
# NAS_SERVICE_NAME=nas-web
# Optional: explicit restart command if PATH/systemctl detection fails
# NAS_RESTART_COMMAND=/usr/bin/systemctl restart nas-web
